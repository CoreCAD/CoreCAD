# CoreCAD branding asset allowlist
# Each line is a glob pattern (relative to the assets repo root) for files
# that cmake is allowed to overlay onto the build tree. Files not matching
# any pattern are ignored. Lines starting with # are comments.
#
# cmake reads this file at configure time via cMake/CoreCAD_Branding.cmake.
# The source tree is never modified — overrides only exist in the build tree.
#
# Note: patterns without a matching corecad_stage_resources() call in cmake
# are treated as documentation only (e.g. installer icons that already live
# in the source tree and don't need build-tree staging).

# Core application icons, splash screen and about images (QRC-compiled)
src/Gui/Icons/*.svg
src/Gui/Icons/*.png

# Windows installer icons (referenced directly from source tree by NSIS)
package/WindowsInstaller/icons/*.ico
package/WindowsInstaller/icons/*.bmp
